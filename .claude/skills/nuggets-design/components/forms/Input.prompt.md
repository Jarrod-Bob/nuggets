Standard text field: 44px tall, 14px radius, ink-200 border that goes golden on focus with a ketchup focus glow.

```jsx
<Input label="Nugget title" placeholder="What's the idea?" value={v} onChange={e => set(e.target.value)} />
```
