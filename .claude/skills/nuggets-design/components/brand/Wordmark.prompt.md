The wordmark lockup — always lowercase "nuggets", never title case.

```jsx
<Wordmark size={28} tone="ink" />
```

Use `tone="cream"` on ink or golden backgrounds. Drop `withMark` when space is tight (mobile headers). `iconOnly` renders just the single nugget mark with no text — the closest thing this system has to a logomark.
