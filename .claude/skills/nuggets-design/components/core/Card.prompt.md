Neutral container with the house 20px radius, 1.5px warm border and a soft warm shadow.

```jsx
<Card tone="cream" interactive onClick={open}>…</Card>
```

`ink` is for one high-contrast panel per screen (upsell, stat block). Never stack two ink cards side by side.
