Round icon-only button — toolbars, card overflow menus, the mobile app bar. Always pass `label`.

```jsx
<IconButton label="More" variant="ghost"><i data-lucide="more-horizontal"></i></IconButton>
```

`filled` is golden and reserved for one affirmative action per surface (e.g. the capture button).
