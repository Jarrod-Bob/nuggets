Tag filter row above the list. Always leads with an "All" reset chip; clicking the active tag clears it.

```jsx
<TagFilter tags={[{name:'go',count:12},{name:'saas',count:5}]} value={tag} onChange={setTag} />
```
