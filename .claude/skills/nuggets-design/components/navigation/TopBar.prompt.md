60px header on cream with a hairline bottom border. No shadow — borders carry the structure in this system.

```jsx
<TopBar center={<SearchField style={{width:360}} />} right={<><RandomNugget/><Button>Drop a nugget</Button></>} />
```
