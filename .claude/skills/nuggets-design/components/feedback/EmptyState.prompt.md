Empty state. Headline is encouraging, never apologetic — "Nothing in here yet", then tell them what to do.

```jsx
<EmptyState variant="bucket" headline="This bucket's empty" body="Drop your first nugget in and it'll show up here." action={<Button>Drop a nugget</Button>} />
```
